<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.theostanton.lfgss"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>


        <activity android:name=".huddle.HuddleActivity"/>
        <activity android:name=".huddles.HuddlesActivity"/>
        <activity android:name=".auth.PersonaActivity"/>
        <activity android:name=".auth.PersonaLoginActivity"/>
        <activity android:name=".category.CatActivity"/>
        <activity android:name=".conversation.ConActivity"/>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="7c5352fd71542e15a27870ef19620f06952dce62" />

    </application>

</manifest>
